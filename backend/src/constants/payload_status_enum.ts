/* This code is defining an enum called `PayloadStatus` and exporting it for use in other modules. The
enum has two members: `UNENCRYPTED` and `ENCRYPTED`, both of which are assigned string values. This
enum can be used to represent the status of a payload, where it can either be unencrypted or
encrypted. */
export enum PayloadStatus {
  UNENCRYPTED = "UNENCRYPTED",
  ENCRYPTED = "ENCRYPTED",
}
