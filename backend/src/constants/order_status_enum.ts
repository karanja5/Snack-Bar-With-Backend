/* This code is defining an enum called `OrderStatus` and exporting it for use in other modules. The
enum has three members: `NEW`, `PENDING`, and `PAID`, each with a corresponding string value. This
enum can be used to represent the status of an order in an application. */
export enum OrderStatus {
  NEW = "NEW",
  PENDING = "PENDING PAYMENT",
  PAID = "PAID",
}
