/* The class "User" defines properties for a user object including their id, name, email, password,
address, isAdmin status, and token. */
export class User {
  id!: string;
  name!: string;
  email!: string;
  password!: string;
  address!: string;
  isAdmin!: boolean;
  token!: string;
}
