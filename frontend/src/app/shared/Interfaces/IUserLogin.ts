/* This code is defining an interface named `IUserLogin` which has two properties: `email` and
`password`, both of type `string`. The `export` keyword makes this interface available for use in
other modules. */
export interface IUserLogin {
  phoneNumber: string;
  password: string;
}
